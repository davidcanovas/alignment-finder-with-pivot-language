# alignment-finder-with-pivot-language
This tool generates alignment files after analyzing two bitexts with a common language.

# Usage

Just run the script with all four files on the same directory.
